import 'package:flutter/material.dart';

class ControlesDeTiempo extends StatelessWidget {
  const ControlesDeTiempo({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
