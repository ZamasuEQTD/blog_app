import 'package:flutter/material.dart';

class SeleccionableBottomSheet extends StatelessWidget {
  const SeleccionableBottomSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const CustomScrollView(
      slivers: [],
    );
  }
}
